language: node_js
node_js:
  - "6"
before_install:
- curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.2.1
- export PATH="$HOME/.yarn/bin:$PATH"
before_script:
  - npm install -g sails
cache: yarn
script: sails www && npm test
